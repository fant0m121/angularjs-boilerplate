import { CheckboxComponent } from './checkbox.component';

export const CheckboxModule = angular
    .module('angularjs-boilerplate.checkbox', [])
    .component('checkbox', CheckboxComponent)
    .name;
